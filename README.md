# react-js-pure-project
Project With All good functions of ReactJS simple

After download the project, you just have to enter the folder and execute the following commands:

npm i
npm start
npm run build to run a dev build  